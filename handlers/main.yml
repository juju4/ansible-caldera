---

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: yes
  when:
    - not is_container | bool

- name: Restart caldera
  ansible.builtin.service:
    name: caldera
    state: restarted
  when:
    - not is_container | bool
